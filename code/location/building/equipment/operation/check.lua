local check = {
  transmitter = {},
  terminal = {},
}

function check.transmitter.broadcast(player)
  
end

function check.transmitter.retune(player)
  
end

--function check.terminal.doSomething()

return check